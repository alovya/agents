# Interactive-session instructions

These apply only to interactive sessions, on top of the shared instructions in AGENTS.md. Autonomous or non-interactive agents are not intended to receive this file, because they run each agent as a direct binary call that bypasses the shell wrappers that inject it.

## Git and workflow

- Commit messages: concise yet meaningful.
- When implementing a plan with a to-do list, ask me to review each finished item, then commit once approved.
